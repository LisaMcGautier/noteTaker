{
  "name": "note-taker",
  "version": "1.0.0",
  "description": "This application uses an express backend to save and retrieve note data from a JSON file and renders to the front end.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "watch": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.16.4",
    "nodemon": "^2.0.4"
  }
}
